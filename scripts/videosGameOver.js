function showBackgroundVideo(video) {
    video.show();
    video.play();
    video.time(55);
    //video.position(width * -4, height * -4);
}

function hideBackgroundVideo(video) {
    video.hide();
}